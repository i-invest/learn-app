import { ArticleSchema } from "./ArticleSchema";
import { PodcastSchema } from "./PodcastSchema";
import { VideoSchema } from "./VideoSchema";

export const schemaTypes = [
	ArticleSchema, PodcastSchema, VideoSchema
];